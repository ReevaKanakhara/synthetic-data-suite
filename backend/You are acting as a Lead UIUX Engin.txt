You are acting as a Lead UI/UX Engineer. Your goal is to perform a visual and logical "Level 10" overhaul of the Synthetic Data Suite.



* LAYOUT PRESERVATION: You MUST keep the existing element positions exactly as they are in the provided screenshots. Do not move the sidebars, change the grid ratios, or shift the placement of the form/canvas.

* STRUCTURE CONSTRAINT: Maintain the current structure: `app/`, `components/`, `lib/`, `types/`. (No `src/` directory).

* LOGIC CONSTRAINT: All existing props, API calls, and data types (BoundingBox, TaskStatus) MUST be preserved.

I. THE VISUAL PHILOSOPHY: "DEEP VOID & AERO-GLASS"

The app currently has an amateur white background. This must be purged.



1. The Void Palette: Use `#020205` for the absolute background. Use `#0A0A0F` for cards and `#16161E` for elevated glass elements.

2. Accents: Primary accent is Electric Violet (`#8B5CF6`) with a secondary Cyber Cyan (`#06B6D4`).

3. Glassmorphism: Use `backdrop-filter: blur(16px)` and `border: 1px solid rgba(255, 255, 255, 0.05)` for all panels.

4. Floating Background: Implement a `NetworkBackground.tsx` (Canvas API) with drifting 'neural' nodes and lines that react to mouse proximity.

II. CORE FEATURE UPGRADES

1. The 'Neural' Canvas Background (`components/NetworkBackground.tsx`)



* Create a drifting node-and-line network. It must be `position: fixed` at `z-index: 0` so it sits behind the layout without affecting button clicks.

2. Intelligent Input & Validation (`components/GenerateForm.tsx`)



* Gibberish Guard: Detect low-entropy input (nonsense strings like 'asdfg').

* The UX: Block the submit button and show a Sonner Toast: "Detecting low-entropy input. Let's add some creative flair to that prompt!"

* Aesthetics: Use a monospace font for the textarea and add a violet glow on focus.

3. Interactive Empty State (`components/EmptyState.tsx`)



* Clickable Chips: Replace the static "No images" list with interactive chips.

* Logic: When a chip is clicked, it must populate the `GenerateForm` and immediately trigger the generation.

* No Emojis: Replace all emojis with `lucide-react` icons (e.g., `Sparkles`, `Binary`, `Box`).

4. Scanning Shimmer (`components/TaskPoller.tsx`)



* While generating, the shimmer card should have a Cyan Laser-Scan line moving vertically to indicate active GPU processing.

5. Floating Aero Panels (`app/annotate/[id]/page.tsx`)



* The Bobbing Effect: The "Image Info" and "How to Annotate" sidebars must use `framer-motion` for a subtle, infinite Y-axis bobbing animation (±5px).

* Glass Texture: They must look like floating panes of glass in a void.

6. Neon Canvas Studio (`components/AnnotationCanvas.tsx`)



* Neon Boxes: Drawn boxes should have glowing borders.

* Interaction: Implement "Right-click to delete" and a "Hover tooltip" showing real-image dimensions.

III. FULL CODE REWRITES (MAINTAINING CURRENT LAYOUT)

Please provide the code for these files:



1. `app/globals.css`: Complete void-reset. Remove all default Next.js white/light styling.

2. `app/layout.tsx`: Integrate the `NetworkBackground` and `Toaster`.

3. `components/NetworkBackground.tsx`: (New) Drifting node canvas.

4. `components/EmptyState.tsx`: (New) Interactive chip system.

5. `components/GenerateForm.tsx`: Updated with validation and neon focus.

6. `app/page.tsx`: Dashboard with the new `EmptyState`.

7. `components/TaskPoller.tsx`: Laser-scan shimmer.

8. `components/AnnotationCanvas.tsx`: Neon boxes and right-click delete.

9. `app/annotate/[id]/page.tsx`: Floating glass sidebar layout.

FINAL POLISH INSTRUCTIONS:



* Micro-animations: Every button gets `whileHover={{ scale: 1.02 }}`.

* Transitions: Use `AnimatePresence` for smooth mounting of generation results.

* Professionalism: Ensure the UI feels like a top-tier research tool (e.g., Tesla Autopilot or OpenAI Playground).



You  i already built everything right? THIS IS THE UI BUT IT LOOKS not so good. Hence i gave you the prompt so that you can refine like the above prompt.,